sudo: false # docker-based container

language: python

python:
  - 3.6

scala:
  - 2.11.2

cache:
  directories:
    - $HOME/.ivy2/cache
    - $HOME/.sbt/boot

script:
  - make build_compiler
  - make test
